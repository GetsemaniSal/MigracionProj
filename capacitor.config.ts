import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'migracionProj',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
